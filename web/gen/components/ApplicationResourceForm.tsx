/**
 * Copyright 2025 Adobe. All rights reserved.
 * This file is licensed to you under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License. You may obtain a copy
 * of the License at http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under
 * the License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR REPRESENTATIONS
 * OF ANY KIND, either express or implied. See the License for the specific language
 * governing permissions and limitations under the License.
 */

// Code generated by Aquarium buf-gen-web-ui-components. DO NOT EDIT.

import React, { useState, useEffect, useRef } from 'react';
import { create } from '@bufbuild/protobuf';
import { ApplicationResourceSchema, type ApplicationResource } from '../../gen/aquarium/v2/application_pb';
import * as yaml from 'js-yaml';
import * as Components from './index';
import { useStreaming } from '../../app/contexts/StreamingContext';

interface ApplicationResourceFormProps {
  mode: 'create' | 'edit' | 'view';
  initialData?: ApplicationResource;
  onSubmit: (data: ApplicationResource) => void;
  onCancel: () => void;
  title?: string;
  readonly?: boolean;
  nested?: boolean;
  onRegister?: (getData: () => any) => void;
}

interface ApplicationResourceFormState {
  uid: string;
  createdAt: string;
  updatedAt: string;
  applicationUid: string;
  nodeUid: string;
  labelUid: string;
  definitionIndex: number;
  identifier: string;
  ipAddr: string;
  hwAddr: string;
  metadata: Record<string, any>;
  timeout: string;
  authentication: any;
}
const defaultApplicationResourceState: ApplicationResourceFormState = {
  uid: '',
  createdAt: '',
  updatedAt: '',
  applicationUid: '',
  nodeUid: '',
  labelUid: '',
  definitionIndex: 0,
  identifier: '',
  ipAddr: '',
  hwAddr: '',
  metadata: {},
  timeout: undefined,
  authentication: undefined,
};


export const ApplicationResourceForm: React.FC<ApplicationResourceFormProps> = ({
  initialData,
  onSubmit,
  onCancel,
  mode,
  title,
  readonly,
  nested = false,
  onRegister
}) => {
  const [formData, setFormData] = useState<ApplicationResourceFormState>(defaultApplicationResourceState);
  const [yamlText, setYamlText] = useState('');
  const [showYamlInput, setShowYamlInput] = useState(false);
  const [yamlError, setYamlError] = useState<string | null>(null);
  const [validationErrors, setValidationErrors] = useState<Record<string, string>>({});
  const [autofillMode, setAutofillMode] = useState<Record<string, 'dropdown' | 'text'>>({});
  const [structFieldText, setStructFieldText] = useState<Record<string, string>>({});
  const [structFieldErrors, setStructFieldErrors] = useState<Record<string, string>>({});
  const { data } = useStreaming();

  // Store references to nested component getData functions
  const nestedGetDataFns = useRef<Record<string, () => any>>({});

  // Initialize form data from initialData
  useEffect(() => {
    if (initialData) {
      const newFormData: ApplicationResourceFormState = {
        uid: initialData.uid || '',
        createdAt: initialData.createdAt ? new Date(Number(initialData.createdAt.seconds) * 1000).toISOString().slice(0, 16) : '',
        updatedAt: initialData.updatedAt ? new Date(Number(initialData.updatedAt.seconds) * 1000).toISOString().slice(0, 16) : '',
        applicationUid: initialData.applicationUid || '',
        nodeUid: initialData.nodeUid || '',
        labelUid: initialData.labelUid || '',
        definitionIndex: initialData.definitionIndex || 0,
        identifier: initialData.identifier || '',
        ipAddr: initialData.ipAddr || '',
        hwAddr: initialData.hwAddr || '',
        metadata: initialData.metadata || {},
        timeout: initialData.timeout ? new Date(Number(initialData.timeout.seconds) * 1000).toISOString().slice(0, 16) : '',
        authentication: initialData.authentication && typeof initialData.authentication === 'object' ? initialData.authentication : undefined,
      };
      setFormData(newFormData);

      // Initialize struct field text
      const newStructText: Record<string, string> = {};
      newStructText['metadata'] = initialData.metadata ? yaml.dump(initialData.metadata) : '';
      setStructFieldText(newStructText);
    }
  }, [initialData]);

  // Register getData function with parent if nested
  useEffect(() => {
    if (nested && onRegister) {
      onRegister(getData);
    }
  }, [nested, onRegister]);

  // Function to collect data from this component and all nested components
  const getData = () => {
    const currentFormData = { ...formData };

    // Collect data from nested components
    Object.keys(nestedGetDataFns.current).forEach(key => {
      const nestedGetData = nestedGetDataFns.current[key];
      if (nestedGetData) {
        // Handle array items: key format is "fieldName[index]"
        const arrayMatch = key.match(/^(.+?)\[(\d+)\]$/);
        if (arrayMatch) {
          const fieldName = arrayMatch[1];
          const index = parseInt(arrayMatch[2], 10);
          if (!Array.isArray(currentFormData[fieldName as keyof ApplicationResourceFormState])) {
            currentFormData[fieldName as keyof ApplicationResourceFormState] = [] as any;
          }
          (currentFormData[fieldName as keyof ApplicationResourceFormState] as any[])[index] = nestedGetData();
        } else if (key.includes('.')) {
          // Handle map items: key format is "fieldName.mapKey"
          const dotIndex = key.indexOf('.');
          const fieldName = key.substring(0, dotIndex);
          const mapKey = key.substring(dotIndex + 1);
          if (!currentFormData[fieldName as keyof ApplicationResourceFormState] || typeof currentFormData[fieldName as keyof ApplicationResourceFormState] !== 'object') {
            currentFormData[fieldName as keyof ApplicationResourceFormState] = {} as any;
          }
          (currentFormData[fieldName as keyof ApplicationResourceFormState] as any)[mapKey] = nestedGetData();
        } else {
          // Regular nested field
          currentFormData[key as keyof ApplicationResourceFormState] = nestedGetData();
        }
      }
    });

    return currentFormData;
  };

  // Register a nested component's getData function
  const registerNestedGetData = (fieldName: string, getDataFn: () => any) => {
    nestedGetDataFns.current[fieldName] = getDataFn;
  };

  // Unregister a nested component's getData function
  const unregisterNestedGetData = (fieldName: string) => {
    delete nestedGetDataFns.current[fieldName];
  };


  // Toggle YAML view and sync with current form data
const handleToggleYamlView = () => {
  if (!showYamlInput) {
    // Switching to YAML view - export current form data to YAML
    try {
      const data = create(ApplicationResourceSchema, {
        uid: formData.uid,
        createdAt: formData.createdAt ? { seconds: BigInt(Math.floor(new Date(formData.createdAt).getTime() / 1000)) } : undefined,
        updatedAt: formData.updatedAt ? { seconds: BigInt(Math.floor(new Date(formData.updatedAt).getTime() / 1000)) } : undefined,
        applicationUid: formData.applicationUid,
        nodeUid: formData.nodeUid,
        labelUid: formData.labelUid,
        definitionIndex: formData.definitionIndex,
        identifier: formData.identifier,
        ipAddr: formData.ipAddr,
        hwAddr: formData.hwAddr,
        metadata: formData.metadata,
        timeout: formData.timeout ? { seconds: BigInt(Math.floor(new Date(formData.timeout).getTime() / 1000)) } : undefined,
        authentication: formData.authentication || undefined,
      });

      // Convert protobuf to plain object for YAML
      const plainObj = JSON.parse(JSON.stringify(data, (key, value) =>
        typeof value === 'bigint' ? value.toString() : value
      ));

      const yamlStr = yaml.dump(plainObj, { indent: 2, lineWidth: -1 });
      setYamlText(yamlStr);
      setYamlError(null);
    } catch (error) {
      setYamlError('Failed to convert to YAML: ' + error);
    }
  }
  setShowYamlInput(!showYamlInput);
};

// Load from YAML (apply changes)
const handleYamlApply = () => {
  try {
    setYamlError(null);
    const parsedData = yaml.load(yamlText) as any;

    if (!parsedData || typeof parsedData !== 'object') {
      throw new Error('Invalid YAML format');
    }

    const newFormData: ApplicationResourceFormState = { ...defaultApplicationResourceState };
    if (parsedData.uid !== undefined) {
      newFormData.uid = parsedData.uid;
    }
    if (parsedData.createdAt !== undefined) {
      if (typeof parsedData.createdAt === 'string') {
        newFormData.createdAt = new Date(parsedData.createdAt).toISOString().slice(0, 16);
      }
    }
    if (parsedData.updatedAt !== undefined) {
      if (typeof parsedData.updatedAt === 'string') {
        newFormData.updatedAt = new Date(parsedData.updatedAt).toISOString().slice(0, 16);
      }
    }
    if (parsedData.applicationUid !== undefined) {
      newFormData.applicationUid = parsedData.applicationUid;
    }
    if (parsedData.nodeUid !== undefined) {
      newFormData.nodeUid = parsedData.nodeUid;
    }
    if (parsedData.labelUid !== undefined) {
      newFormData.labelUid = parsedData.labelUid;
    }
    if (parsedData.definitionIndex !== undefined) {
      newFormData.definitionIndex = parsedData.definitionIndex;
    }
    if (parsedData.identifier !== undefined) {
      newFormData.identifier = parsedData.identifier;
    }
    if (parsedData.ipAddr !== undefined) {
      newFormData.ipAddr = parsedData.ipAddr;
    }
    if (parsedData.hwAddr !== undefined) {
      newFormData.hwAddr = parsedData.hwAddr;
    }
    if (parsedData.metadata !== undefined) {
      if (typeof parsedData.metadata === 'object') {
        newFormData.metadata = parsedData.metadata;
      }
    }
    if (parsedData.timeout !== undefined) {
      if (typeof parsedData.timeout === 'string') {
        newFormData.timeout = new Date(parsedData.timeout).toISOString().slice(0, 16);
      }
    }
    if (parsedData.authentication !== undefined) {
      newFormData.authentication = parsedData.authentication;
    }

    setFormData(newFormData);
    setShowYamlInput(false);
  } catch (error) {
    setYamlError('YAML parsing error: ' + error);
  }
};

// Copy YAML to clipboard
const handleCopyYaml = () => {
  navigator.clipboard.writeText(yamlText).then(() => {
    alert('YAML copied to clipboard!');
  }).catch(() => {
    alert('Failed to copy to clipboard');
  });
};

// Validate form data
const validateForm = (): boolean => {
  const errors: Record<string, string> = {};
  if (mode !== 'create' && (!formData.uid)) {
    errors.uid = 'Uid is required';
  }
  if (mode !== 'create' && (!formData.createdAt)) {
    errors.createdAt = 'Created At is required';
  }
  if (mode !== 'create' && (!formData.updatedAt)) {
    errors.updatedAt = 'Updated At is required';
  }
  if (!formData.applicationUid) {
    errors.applicationUid = 'Application Uid is required';
  }
  if (!formData.nodeUid) {
    errors.nodeUid = 'Node Uid is required';
  }
  if (!formData.labelUid) {
    errors.labelUid = 'Label Uid is required';
  }
  if (!formData.definitionIndex) {
    errors.definitionIndex = 'Definition Index is required';
  }
  if (!formData.identifier) {
    errors.identifier = 'Identifier is required';
  }
  if (!formData.ipAddr) {
    errors.ipAddr = 'Ip Addr is required';
  }
  if (!formData.hwAddr) {
    errors.hwAddr = 'Hw Addr is required';
  }
  if (!formData.metadata) {
    errors.metadata = 'Metadata is required';
  }

  setValidationErrors(errors);
  return Object.keys(errors).length === 0;
};

// Handle form submission
const handleSubmit = () => {
  if (!validateForm()) {
    return;
  }

  try {
    // Collect data from this component and all nested components
    const collectedData = getData();

    // Convert form data to protobuf message
    const data = create(ApplicationResourceSchema, {
      uid: collectedData.uid,
      createdAt: collectedData.createdAt ? { seconds: BigInt(Math.floor(new Date(collectedData.createdAt).getTime() / 1000)) } : undefined,
      updatedAt: collectedData.updatedAt ? { seconds: BigInt(Math.floor(new Date(collectedData.updatedAt).getTime() / 1000)) } : undefined,
      applicationUid: collectedData.applicationUid,
      nodeUid: collectedData.nodeUid,
      labelUid: collectedData.labelUid,
      definitionIndex: collectedData.definitionIndex,
      identifier: collectedData.identifier,
      ipAddr: collectedData.ipAddr,
      hwAddr: collectedData.hwAddr,
      metadata: collectedData.metadata,
      timeout: collectedData.timeout ? { seconds: BigInt(Math.floor(new Date(collectedData.timeout).getTime() / 1000)) } : undefined,
      authentication: collectedData.authentication || undefined,
    });

    onSubmit(data);
  } catch (error) {
    setYamlError('Failed to create ApplicationResource: ' + error);
  }
};

// Handle field changes
const handleFieldChange = (field: string, value: any) => {
  setFormData(prev => ({
    ...prev,
    [field]: value
  }));

  // Clear validation error for this field
  if (validationErrors[field]) {
    setValidationErrors(prev => ({
      ...prev,
      [field]: ''
    }));
  }
};

// Handle array field changes
const handleArrayChange = (field: string, index: number, value: any) => {
  setFormData(prev => ({
    ...prev,
    [field]: prev[field as keyof ApplicationResourceFormState].map((item: any, i: number) =>
      i === index ? value : item
    )
  }));
};

// Add array item
const addArrayItem = (field: string, defaultValue: any) => {
  setFormData(prev => ({
    ...prev,
    [field]: [...(prev[field as keyof ApplicationResourceFormState] as any[]), defaultValue]
  }));
};

// Remove array item
const removeArrayItem = (field: string, index: number) => {
  // Unregister the nested component
  unregisterNestedGetData(field + '[' + index + ']');

  // Remove from formData
  setFormData(prev => ({
    ...prev,
    [field]: (prev[field as keyof ApplicationResourceFormState] as any[]).filter((_, i) => i !== index)
  }));

  // Re-register remaining items with updated indices
  setTimeout(() => {
    const remainingItems = (formData[field as keyof ApplicationResourceFormState] as any[]).length - 1;
    for (let i = index; i < remainingItems; i++) {
      const oldKey = field + '[' + (i + 1) + ']';
      const newKey = field + '[' + i + ']';
      if (nestedGetDataFns.current[oldKey]) {
        nestedGetDataFns.current[newKey] = nestedGetDataFns.current[oldKey];
        delete nestedGetDataFns.current[oldKey];
      }
    }
  }, 0);
};

const isReadOnly = readonly || mode === 'view';
const formTitle = title || (mode === 'create' ? 'Create ApplicationResource' : mode === 'edit' ? 'Edit ApplicationResource' : 'View ApplicationResource');

// Helper function to get autofill options
const getAutofillOptions = (type: string) => {
  switch (type.toLowerCase()) {
    case 'label':
      return data.labels || [];
    case 'user':
      return data.users || [];
    case 'role':
      return data.roles || [];
    case 'node':
      return data.nodes || [];
    default:
      return [];
  }
};

// Helper function to parse struct field
const parseStructField = (fieldName: string, text: string) => {
  try {
    const parsed = yaml.load(text);
    if (parsed && typeof parsed === 'object') {
      handleFieldChange(fieldName, parsed);
      setStructFieldErrors(prev => ({ ...prev, [fieldName]: '' }));
    } else {
      setStructFieldErrors(prev => ({ ...prev, [fieldName]: 'Invalid YAML/JSON format' }));
    }
  } catch (error) {
    setStructFieldErrors(prev => ({ ...prev, [fieldName]: 'Parse error: ' + error }));
  }
};

// Helper function to determine if field is simple (can be inline)
const isSimpleField = (field: any) => {
  return !field.IsRepeated && !field.IsMap && !field.IsMessage && !field.IsStruct && !field.IsTimestamp && !field.AutofillType;
};


  return (
    <div className="space-y-4">
      {!nested && (
        <div className="flex justify-between items-center">
          <h2 className="text-xl font-semibold text-gray-900 dark:text-white">
            {formTitle}
          </h2>
          <div className="flex space-x-2">
            <button
              onClick={handleToggleYamlView}
              className="px-3 py-1 text-sm bg-blue-100 text-blue-800 rounded-md hover:bg-blue-200 dark:bg-blue-900 dark:text-blue-200"
            >
              {showYamlInput ? 'Hide YAML' : 'View YAML'}
            </button>
          </div>
        </div>
      )}

      {!nested && showYamlInput && (
        <div className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
              YAML View
            </label>
            <textarea
              value={yamlText}
              onChange={(e) => setYamlText(e.target.value)}
              disabled={isReadOnly}
              className="w-full h-96 px-3 py-2 border border-gray-300 rounded-md font-mono text-sm dark:bg-gray-700 dark:border-gray-600 dark:text-white"
              placeholder="YAML configuration..."
            />
          </div>
          <div className="flex space-x-2">
            <button
              onClick={handleCopyYaml}
              className="px-4 py-2 text-sm bg-green-600 text-white rounded-md hover:bg-green-700"
            >
              Copy to Clipboard
            </button>
            {!isReadOnly && (
              <button
                onClick={handleYamlApply}
                className="px-4 py-2 text-sm bg-blue-600 text-white rounded-md hover:bg-blue-700"
              >
                Apply Changes
              </button>
            )}
            <button
              onClick={() => setShowYamlInput(false)}
              className="px-4 py-2 text-sm bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200 dark:bg-gray-700 dark:text-gray-300"
            >
              Close
            </button>
          </div>
          {yamlError && (
            <div className="text-sm text-red-600 dark:text-red-400">
              {yamlError}
            </div>
          )}
        </div>
      )}

      {(!nested || !showYamlInput) && (
        <div className="space-y-3">
{/* Uid field */}
  {!(mode === 'create' && true) && (
    <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Uid *
    </label>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.uid}
      onChange={(e) => handleFieldChange('uid', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && true)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.uid && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.uid}
      </div>
    )}
  </div>
</div>

    </div>
  )}{/* Created At field */}
  {!(mode === 'create' && true) && (
    <div>
      {/* Complex field - traditional layout */}
      <div className="space-y-2">
<div className="flex items-center space-x-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    Created At *
  </label>
</div>

<input
  type="datetime-local"
  value={formData.createdAt}
  onChange={(e) => handleFieldChange('createdAt', e.target.value)}
  disabled={isReadOnly || (mode === 'edit' && true)}
  className="w-full px-3 py-2 border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
/>

{validationErrors.createdAt && (
  <div className="text-sm text-red-600 dark:text-red-400 mt-1">
    {validationErrors.createdAt}
  </div>
)}

      </div>
    </div>
  )}{/* Updated At field */}
  {!(mode === 'create' && true) && (
    <div>
      {/* Complex field - traditional layout */}
      <div className="space-y-2">
<div className="flex items-center space-x-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    Updated At *
  </label>
</div>

<input
  type="datetime-local"
  value={formData.updatedAt}
  onChange={(e) => handleFieldChange('updatedAt', e.target.value)}
  disabled={isReadOnly || (mode === 'edit' && true)}
  className="w-full px-3 py-2 border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
/>

{validationErrors.updatedAt && (
  <div className="text-sm text-red-600 dark:text-red-400 mt-1">
    {validationErrors.updatedAt}
  </div>
)}

      </div>
    </div>
  )}{/* Application Uid field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Application Uid *
    </label>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.applicationUid}
      onChange={(e) => handleFieldChange('applicationUid', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.applicationUid && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.applicationUid}
      </div>
    )}
  </div>
</div>

  </div>{/* Node Uid field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Node Uid *
    </label>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.nodeUid}
      onChange={(e) => handleFieldChange('nodeUid', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.nodeUid && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.nodeUid}
      </div>
    )}
  </div>
</div>

  </div>{/* Label Uid field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Label Uid *
    </label>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.labelUid}
      onChange={(e) => handleFieldChange('labelUid', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.labelUid && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.labelUid}
      </div>
    )}
  </div>
</div>

  </div>{/* Definition Index field */}
  <div>
{/* Simple number field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Definition Index *
    </label>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="number"
      value={formData.definitionIndex}
      onChange={(e) => handleFieldChange('definitionIndex', parseInt(e.target.value) || 0)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.definitionIndex && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.definitionIndex}
      </div>
    )}
  </div>
</div>

  </div>{/* Identifier field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Identifier *
    </label>
    <div className="relative group">
      <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
      <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
        <pre className="whitespace-pre-wrap text-xs leading-relaxed">Unique for driver identifier of the instance which can be used to find it later.</pre>
      </div>
    </div>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.identifier}
      onChange={(e) => handleFieldChange('identifier', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.identifier && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.identifier}
      </div>
    )}
  </div>
</div>

  </div>{/* Ip Addr field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Ip Addr *
    </label>
    <div className="relative group">
      <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
      <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
        <pre className="whitespace-pre-wrap text-xs leading-relaxed">Is a weak current network IP address of the instance, usually gathered based on the `hw_addr`.</pre>
      </div>
    </div>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.ipAddr}
      onChange={(e) => handleFieldChange('ipAddr', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.ipAddr && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.ipAddr}
      </div>
    )}
  </div>
</div>

  </div>{/* Hw Addr field */}
  <div>
{/* Simple string field - inline layout */}
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-2 min-w-0 flex-1">
    <label className="text-sm font-medium text-gray-700 dark:text-gray-300 whitespace-nowrap">
      Hw Addr *
    </label>
    <div className="relative group">
      <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
      <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
        <pre className="whitespace-pre-wrap text-xs leading-relaxed">MAC or any other type of network address which will allow to properly identify the node through network interaction.</pre>
      </div>
    </div>
  </div>
  <div className="flex-1 max-w-xs ml-4">
    <input
      type="text"
      value={formData.hwAddr}
      onChange={(e) => handleFieldChange('hwAddr', e.target.value)}
      disabled={isReadOnly || (mode === 'edit' && false)}
      className="w-full px-3 py-1 text-sm border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
    />
    {validationErrors.hwAddr && (
      <div className="text-xs text-red-600 dark:text-red-400 mt-1">
        {validationErrors.hwAddr}
      </div>
    )}
  </div>
</div>

  </div>{/* Metadata field */}
  <div>
    {/* Complex field - traditional layout */}
    <div className="space-y-2">
<div className="flex items-center space-x-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    Metadata *
  </label>
  <div className="relative group">
    <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
    <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
      <pre className="whitespace-pre-wrap text-xs leading-relaxed">Combined Application and Label metadata (in this order) to make it available through `Meta API` to the instance. example: JENKINS_URL: &#39;http://172.16.1.1:8085/&#39; JENKINS_AGENT_SECRET: 03839eabcf945b1e780be8f9488d264c4c57bf388546da9a84588345555f29b0 JENKINS_AGENT_NAME: test-node JENKINS_AGENT_WORKSPACE: /Volumes/ws</pre>
    </div>
  </div>
</div>

<div className="border border-gray-300 rounded-md p-3 dark:border-gray-600 space-y-2">
  <textarea
    value={structFieldText.metadata || ''}
    onChange={(e) => setStructFieldText(prev => ({ ...prev, metadata: e.target.value }))}
    onBlur={() => parseStructField('metadata', structFieldText.metadata || '')}
    disabled={isReadOnly || (mode === 'edit' && false)}
    className="w-full h-32 px-3 py-2 border-0 font-mono text-sm dark:bg-gray-700 dark:text-white resize-none"
    placeholder="Enter YAML or JSON object..."
  />
  {structFieldErrors.metadata && (
    <div className="text-sm text-red-600 dark:text-red-400">
      {structFieldErrors.metadata}
    </div>
  )}
  {!isReadOnly && !(mode === 'edit' && false) && (
    <button
      type="button"
      onClick={() => parseStructField('metadata', structFieldText.metadata || '')}
      className="px-3 py-1 text-sm bg-blue-100 text-blue-800 rounded-md hover:bg-blue-200"
    >
      Parse & Update
    </button>
  )}
</div>

{validationErrors.metadata && (
  <div className="text-sm text-red-600 dark:text-red-400 mt-1">
    {validationErrors.metadata}
  </div>
)}

    </div>
  </div>{/* Timeout field */}
  <div>
    {/* Complex field - traditional layout */}
    <div className="space-y-2">
<div className="flex items-center space-x-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    Timeout
  </label>
  <div className="relative group">
    <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
    <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
      <pre className="whitespace-pre-wrap text-xs leading-relaxed">Calculated during allocation time of when the Resource have to die anyway</pre>
    </div>
  </div>
</div>

<input
  type="datetime-local"
  value={formData.timeout}
  onChange={(e) => handleFieldChange('timeout', e.target.value)}
  disabled={isReadOnly || (mode === 'edit' && false)}
  className="w-full px-3 py-2 border border-gray-300 rounded-md dark:bg-gray-700 dark:border-gray-600 dark:text-white"
/>

{validationErrors.timeout && (
  <div className="text-sm text-red-600 dark:text-red-400 mt-1">
    {validationErrors.timeout}
  </div>
)}

    </div>
  </div>{/* Authentication field */}
  <div>
    {/* Complex field - traditional layout */}
    <div className="space-y-2">
<div className="flex items-center space-x-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    Authentication
  </label>
  <div className="relative group">
    <span className="cursor-help text-gray-400 hover:text-gray-600">(?)</span>
    <div className="absolute left-0 bottom-6 bg-gray-800 text-white text-xs rounded-md shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-50 pointer-events-none max-w-sm w-max p-3 min-w-64 max-h-48 overflow-y-auto">
      <pre className="whitespace-pre-wrap text-xs leading-relaxed">Authentication information for connection.</pre>
    </div>
  </div>
</div>

{(() => {
  const ComponentName = 'AuthenticationForm';
  const NestedComponent = (Components as any)[ComponentName];
  // Optional message field - show Add/Remove buttons
  if (!formData.authentication) {
    return (
      <div className="border border-dashed border-gray-300 rounded-md p-6 text-center dark:border-gray-600">
        <div className="text-sm text-gray-500 dark:text-gray-400 mb-3">
          Authentication is not set
        </div>
        {!isReadOnly && !(mode === 'edit' && false) && (
          <button
            type="button"
            onClick={() => handleFieldChange('authentication', {})}
            className="px-4 py-2 text-sm bg-blue-100 text-blue-800 rounded-md hover:bg-blue-200"
          >
            Add Authentication
          </button>
        )}
      </div>
    );
  }

  if (NestedComponent) {
    return (
      <div className="relative border-2 border-gray-200 rounded-lg p-3 dark:border-gray-600 bg-gray-50 dark:bg-gray-800">
        <div className="flex items-center justify-between mb-3 pb-2 border-b border-gray-200 dark:border-gray-600">
          <h4 className="text-sm font-medium text-gray-700 dark:text-gray-300">
            Authentication
          </h4>
          {!isReadOnly && !(mode === 'edit' && false) && (
            <button
              type="button"
              onClick={() => {
                handleFieldChange('authentication', null);
                unregisterNestedGetData('authentication');
              }}
              className="flex items-center justify-center w-6 h-6 text-red-500 hover:text-red-700 hover:bg-red-100 rounded-full transition-colors"
              title="Remove Authentication"
            >
              <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          )}
        </div>
        <div className="pl-2">
          <NestedComponent
            key={JSON.stringify(formData.authentication)}
            mode={mode}
            initialData={formData.authentication}
            onSubmit={() => {}}
            onCancel={() => {}}
            title={'Authentication'}
            readonly={isReadOnly || (mode === 'edit' && false)}
            nested={true}
            onRegister={(getDataFn: () => any) => registerNestedGetData('authentication', getDataFn)}
          />
        </div>
      </div>
    );
  }

  // Fallback to textarea for JSON editing
  return (
    <div className="border border-gray-300 rounded-md p-3 dark:border-gray-600 space-y-2">
      <div className="flex justify-between items-center mb-2">
        <div className="text-sm text-gray-500">
          Authentication (Component not available - using JSON editor)
        </div>
        {!isReadOnly && !(mode === 'edit' && false) && (
          <button
            type="button"
            onClick={() => handleFieldChange('authentication', null)}
            className="flex items-center justify-center w-6 h-6 text-red-500 hover:text-red-700 hover:bg-red-100 rounded-full transition-colors"
            title="Remove Authentication"
          >
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        )}
      </div>
      <textarea
        value={JSON.stringify(formData.authentication, null, 2)}
        onChange={(e) => {
          try {
            const parsed = JSON.parse(e.target.value);
            handleFieldChange('authentication', parsed);
          } catch (error) {
            // Invalid JSON, keep the text value for user to fix
          }
        }}
        disabled={isReadOnly || (mode === 'edit' && false)}
        className="w-full h-32 px-3 py-2 border-0 font-mono text-sm dark:bg-gray-700 dark:text-white resize-none"
        placeholder="Enter JSON object for Authentication..."
      />
    </div>
  );
})()}

{validationErrors.authentication && (
  <div className="text-sm text-red-600 dark:text-red-400 mt-1">
    {validationErrors.authentication}
  </div>
)}

    </div>
  </div>

        </div>
      )}

      {!nested && (
        <div className="flex justify-end space-x-3 pt-4 border-t border-gray-200 dark:border-gray-700">
          <button
            onClick={onCancel}
            className="px-4 py-2 text-sm bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200"
          >
            {isReadOnly ? 'Close' : 'Cancel'}
          </button>
          {!isReadOnly && (
            <button
              onClick={handleSubmit}
              className="px-4 py-2 text-sm bg-blue-600 text-white rounded-md hover:bg-blue-700"
            >
              {mode === 'create' ? 'Create' : 'Save'}
            </button>
          )}
        </div>
      )}
    </div>
  );
};
